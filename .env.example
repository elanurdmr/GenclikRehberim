# Gençlik Rehberim — Örnek ortam dosyası
# ----------------------------------------------------------------------
# Bu dosyayı `.env` olarak kopyalayın ve değerleri kendi ortamınıza göre
# doldurun. XAMPP varsayılanları zaten aşağıda hazırdır.
#
#   Windows : copy .env.example .env
#   Linux   : cp .env.example .env
# ----------------------------------------------------------------------

DB_HOST=localhost
DB_NAME=db_genclik_rehberim
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
